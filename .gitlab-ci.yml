build-job:
  stage: build
  build:
    stage: docker
    - echo "Job docker is running!"

docker-job:
  stage: docker
  script:
    - echo "Job docker is running!"

deploy-prod:
  stage: deploy
  script:
    - echo "This job deploys something!"
   